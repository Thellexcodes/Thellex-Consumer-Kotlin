<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke android:width="0dp" android:color="@color/darkBlue" />
    <solid android:color="@color/darkBlue" />
    <corners android:radius="6dp" />
</shape>
