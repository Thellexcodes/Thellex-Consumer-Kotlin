<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <solid android:color="@color/darkBlue" />

    <stroke
            android:width="1dp"
            android:color="@color/goldenYellow" />

    <!-- Rounded corners -->
    <corners android:radius="6dp" />
</shape>
